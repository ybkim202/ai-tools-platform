# pre-commit 훅 — 시크릿 커밋을 로컬에서 선차단 (CI의 gitleaks 잡과 동일 설정)
# 설치(1회): pip install pre-commit && pre-commit install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
