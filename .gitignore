
# Claude Code 개인 설정(알림 hook 등)
.claude/settings.local.json

# Python 컴파일 캐시 (히스토리에 키가 박혀 노출됐던 원인 — 절대 커밋 금지)
__pycache__/
*.pyc

# OS / 에디터 부산물
.DS_Store

# 환경변수(시크릿)
.env
.env.*
!.env.example
# backend/env 는 비밀번호를 담을 수 있는 env 파일 — 추적 금지(템플릿도 커밋하지 않는다)
backend/env

# 로고 디자인 소스(대용량 바이너리) — 앱은 frontend/src/assets 사용
logo/*.ai

logo/**/*.ai
.DS_Store
