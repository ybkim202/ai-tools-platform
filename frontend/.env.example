# frontend 환경변수 템플릿
# 사용법: 이 파일을 frontend/.env.local 로 복사하면 된다(.env.local 은 gitignore).
# CRA 규칙상 REACT_APP_ 접두어 변수만 빌드에 주입된다. 시크릿을 넣지 말 것(번들에 노출됨).

# 백엔드 API 베이스 URL — 로컬 개발은 로컬 백엔드를 본다.
# (운영 API를 직접 보는 건 CORS ALLOWED_ORIGINS 화이트리스트 때문에 안 됨)
REACT_APP_API_URL=http://localhost:8000/api
